#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int total,units;
    
    for(int i=0;i<n;i++){
        scanf("%d",&units);
     total=total+units;
    }
    printf("%d",total);
    return 0;
}
